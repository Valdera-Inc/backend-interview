/**
 * Returns the number of paths available
 * from the top left of a matrix to the bottom right.
 * - Only move down and right.
 * - `0`s are free, `1`s are obstacles.
 * - Granted assumptions: (0,0) and (m,n) are never `1`
 * @param {number[][]} matrix
 * @returns {number}
 */
function traverseMatrix(matrix) {
	
}

module.exports = traverseMatrix;
